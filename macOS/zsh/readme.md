## Install Zsh

- Verify zsh installation by running `zsh --version`. Expected result: zsh 5.9 or more recent.
- Test with `$SHELL --version`. Expected result: 'zsh 5.8' or similar
- Make it your default shell: `chsh -s $(which zsh)`
- Restart your computer to start using the new shell.

## oh-my-zsh

Follow the file for more detailed instruction.
